/* const repeatedString = (name: string, repeatNo: number = 3) => {
    if (repeatNo > 0) {
       return name.repeat(repeatNo)
    }
}

 const result=repeatedString("Rafin",2)
 console.log(result); */